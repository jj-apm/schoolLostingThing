module.exports = (app) => {
    // app.beforeStart(async function() {
    //     await app.model.Thank.sync({ alter: true })
    //     app.database = await app.mysql.createInstance(app.config.mysql.client);
    // })
}